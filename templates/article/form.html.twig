{% extends 'base.html.twig' %}

{% block title %}{{ action|trans }} {{ "an article"|trans }}{% endblock %}

{% block body %}

    <h1>{{ action|trans }} {{ "an article"|trans }}</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <input type="submit" value="{{ action|trans }}">
    {{ form_end(form) }}

{% endblock %}
